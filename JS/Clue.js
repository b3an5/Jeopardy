class Clue {

  checkAnswer() {
    //checks if player guess is right 
    //if so run score multiplyer fn in round.js
    //if not move to next player method in round.js
  }

  
}

if (typeof module !== 'undefined') {
  module.exports = Clue;
}